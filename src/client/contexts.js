import React from "react";


// Create contexts for the global state (largely unused),
// audience page, and speaker page
export const AppContext = React.createContext();
export const AudienceContext = React.createContext();
export const SpeakerContext = React.createContext();
